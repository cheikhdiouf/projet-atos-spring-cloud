
server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/cheikhdiouf/config-server-repo
          clone-on-start: true
          default-label: main
  application:
    name: config-service


